# ─── Core ML & Embeddings ───────────────────────────────────────────────
sentence-transformers==3.0.1        # Phase 2: all-MiniLM-L6-v2 embeddings
torch==2.3.1                        # Backend for sentence-transformers
transformers==4.43.3                # HuggingFace tokenizers & model hub

# ─── ML Classifiers ─────────────────────────────────────────────────────
scikit-learn==1.5.1                 # SVM, RandomForest, LogisticRegression, metrics

# ─── Data Handling ──────────────────────────────────────────────────────
pandas==2.2.2                       # CSV I/O, dataframe ops (regex_classified, ml_training)
numpy==1.26.4                       # Array ops, embedding matrices

# ─── Utilities ──────────────────────────────────────────────────────────
tqdm==4.66.4                        # Progress bars for embedding generation
joblib==1.4.2                       # Model serialisation (save/load trained classifier)